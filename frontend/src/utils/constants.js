/**
 * Frontend route constants.
 */

import { API_BASE_URL } from '../config/api';

export { API_BASE_URL };

export const ROUTES = {
  LOGIN: '/login',
  DASHBOARD: '/',
  STUDENTS: '/students',
  COURSES: '/courses',
  ATTENDANCE: '/attendance',
  NOTICES: '/notices',
};
